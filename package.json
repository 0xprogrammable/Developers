{
  "name": "@programmable/developers",
  "version": "2.0.0",
  "private": true,
  "description": "Canonical feeds, schemas, contracts, and guides for Programmable launches.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/0xprogrammable/developers.git"
  },
  "homepage": "https://developers.programmable.family",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke:live": "node scripts/live-smoke.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
